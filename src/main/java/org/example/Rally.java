package org.example;



public class Rally {
    String carWinner =
    int speedWinner = 0;

    static String winner(car) {
        int speedCar = Machine.speedRange();
        String carName = Machine.machineName();
        if (this.car < )
    }
}